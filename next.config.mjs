/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        optimizeCss: true,
        typedRoutes: true,
        serverActions: true,
    },
};

export default nextConfig;
